<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:id="@+id/rw_root"
  android:layout_width="match_parent"
  android:layout_height="match_parent">

  <ImageView
    android:id="@+id/rw_bg_fill"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:contentDescription="@null"
    android:scaleType="fitXY"
    android:src="@drawable/rw_card_fill" />

  <ImageView
    android:id="@+id/rw_bg_stroke"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:contentDescription="@null"
    android:scaleType="fitXY"
    android:src="@drawable/rw_card_stroke" />

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingStart="14dp"
    android:paddingTop="12dp"
    android:paddingEnd="14dp"
    android:paddingBottom="10dp">

    <LinearLayout
      android:id="@+id/rw_header"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:baselineAligned="false"
      android:gravity="center_vertical"
      android:orientation="horizontal">

      <TextView
        android:id="@+id/rw_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:ellipsize="end"
        android:fontFamily="@font/rw_nunito_bold"
        android:includeFontPadding="false"
        android:letterSpacing="0.09"
        android:maxLines="1"
        android:text="TODAY'S VITAMINS"
        android:textColor="#8A7076"
        android:textSize="10sp" />

      <LinearLayout
        android:id="@+id/rw_streak"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="6dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
          android:id="@+id/rw_streak_icon"
          android:layout_width="13dp"
          android:layout_height="13dp"
          android:contentDescription="@null"
          android:src="@drawable/rw_ic_fire" />

        <TextView
          android:id="@+id/rw_streak_text"
          android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          android:layout_marginStart="3dp"
          android:fontFamily="@font/rw_nunito_bold"
          android:includeFontPadding="false"
          android:maxLines="1"
          android:text="12"
          android:textColor="#E5A020"
          android:textSize="11sp" />
      </LinearLayout>
    </LinearLayout>

    <!-- Rows are inflated one per active vitamin, as many as the height allows. -->
    <LinearLayout
      android:id="@+id/rw_rows"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_marginTop="7dp"
      android:orientation="vertical" />

    <TextView
      android:id="@+id/rw_more"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_marginTop="3dp"
      android:fontFamily="@font/rw_nunito_bold"
      android:includeFontPadding="false"
      android:maxLines="1"
      android:text="+2 more"
      android:textColor="#B9A3A8"
      android:textSize="11sp"
      android:visibility="gone" />

    <TextView
      android:id="@+id/rw_empty"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_marginTop="8dp"
      android:fontFamily="@font/rw_nunito_bold"
      android:includeFontPadding="false"
      android:text="Tap to set up your daily list"
      android:textColor="#8A7076"
      android:textSize="12sp"
      android:visibility="gone" />
  </LinearLayout>
</FrameLayout>
